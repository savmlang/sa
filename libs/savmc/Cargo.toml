[package]
name = "savmc"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
postcard = { version = "^1", features = ["alloc"], default-features = false }
savm = { path = "../../savm", default-features = false }
cprelude = { path = "../cprelude" }
