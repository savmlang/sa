[package]
name = "sabin_spec"
version = "0.1.0"
edition = "2024"

[dependencies]
zip = { version = "^8", default-features = false, features = [
  "deflate-flate2",
  "deflate-zopfli",
  "deflate-flate2-zlib-rs",
] }
