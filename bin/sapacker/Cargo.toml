[package]
name = "sapacker"
version = "0.0.1"
description = "Pack your sa codebase into a sabin file"
edition = "2024"

[dependencies]
console = "^0.16"

postcard = { version = "^1", default-features = false, features = ["alloc"] }
rusqlite = { version = "^0.40", features = ["bundled", "blob"] }

sart = { path = "../../sart" }
