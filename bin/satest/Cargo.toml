[package]
name = "satest"
version = "0.0.1"
edition = "2024"

[dependencies]
savm = { path = "../../savm", default-features = false, features = [
  "cranelift",
  "sendback",
] }

serde = { version = "^1", features = ["derive"] }
serde_json = "^1"
toml = "^1"
